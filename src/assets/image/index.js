const images = {
    menubar: require('@/assets/image/menuBar.svg').default,
    search: require('@/assets/image/search.svg').default,
    cart: require('@/assets/image/cart.svg').default,
    user: require('@/assets/image/user.svg').default,
};

export default images;
